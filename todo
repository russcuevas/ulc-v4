PROMPT 

i need in my collector only in Financial Counselor area will do not in Manila Area Caloocan Area or etc what i want is add an column in database
after balance add an savings_balance 

ALTER TABLE clients_loans
ADD COLUMN savings_balance DECIMAL(10,2) NULL AFTER balance;

ALTER TABLE clients_payments
ADD COLUMN savings_amount DECIMAL(10,2) NOT NULL DEFAULT 0.00 AFTER collection;